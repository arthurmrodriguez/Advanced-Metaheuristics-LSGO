#include "Vesicle.h"

Vesicle::Vesicle(int _x, int _y, int _radius, int _width, double _value, int _valid)
           : x(_x), y(_y), radius(_radius), width(_width), value(_value), valid(_valid) {
}

Vesicle::~Vesicle() {
}
